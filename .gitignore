/target
**/*.rs.bk

# Trunk build output (chess-web)
clients/chess-web/dist/
clients/chess-web/dist-static/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Insta pending snapshots
**/*.snap.new

# Local TLS keys + certs (e.g. mkcert for the WebRTC LAN spike).
# NEVER commit *-key.pem — those are private keys.
*.pem

# playwright-cli session artifacts (snapshots, screenshots, traces, etc.)
.playwright-cli/
