//! 三國暗棋 move generation. Implementation deferred to PR 2.

use crate::moves::MoveList;
use crate::state::GameState;

pub fn generate(_state: &GameState, _out: &mut MoveList) {
    // PR-2 placeholder.
}
