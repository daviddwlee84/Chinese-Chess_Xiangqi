# TODO

Long-term backlog for Chinese Chess. See AGENTS.md
for the maintenance workflow that agents should follow.

> **For agents**: when the user surfaces an idea explicitly **not** being
> implemented this session (signals: "maybe later", "nice to have",
> "工程量太大需要再評估", "先記下來"), add it here with priority + effort tags.
> Do not create new `ROADMAP.md` / `IDEAS.md` / `BACKLOG.md` files —
> `TODO.md` is the single backlog index. Long-form research goes in
> [`backlog/<slug>.md`](backlog/).

<!-- Use the exact section order: P1, P2, P3, P?, Done.
     The bundled scripts/todo-kanban.sh validator only inspects top-level
     `- [ ]` and `- ✅` items inside these sections. Prose paragraphs,
     blockquotes, indented sub-bullets, HTML comments, and `---` rules are
     ignored — feel free to add inline guidance like this without breaking
     machine readability. -->

## P1

Likely next batch — items you'd reach for if you sat down to work today.

- [ ] **[M] Three Kingdoms Banqi (三國暗棋) implementation** — Build BoardShape::ThreeKingdom mask + rules/three_kingdom.rs with 3-side capture rules. Architecture supports it (TurnOrder, Side, BoardShape variant exist); rules logic and home-zone layout need spec from canonical source. → [research](backlog/three-kingdoms-banqi.md)
- [ ] **[M] Phase 2 — chains-with-dark-hops (`暗連`)** — DARK_CAPTURE single-hop ships; chains still stop at face-down tiles. Extend `ChainHop` to `captured: Option<Piece>` (None = dark hop), thread through `sanitize_for_observer`, and let `extend_recursive` continue past hidden tiles when both DARK_CAPTURE + CHAIN_CAPTURE are set. Bumps `Move::ChainCapture` wire shape → network protocol bump.
- [ ] **[M] Chain-builder UI for incremental 連吃** — Engine emits atomic `Move::ChainCapture { path }` today. Add a chain-builder state to GameView/NetView (TUI) and the play page (web): click first capture → highlight next-hop options → click again to extend OR Enter to commit. Today the user has to click the *final* hop to commit the full chain; this gives step-by-step visual feedback.
- [ ] **[S] Wire CANNON_FAST_MOVE house rule** — Banqi cannon non-capturing slide goes any number of squares (capture still requires the one-piece jump). Reuse the ray walk from rules/banqi.rs::gen_chariot_rush.
- [ ] **[S] Threefold repetition draw detection** — v5 (2026-05-10) shipped Zobrist hashing on `GameState.position_hash`; this TODO is just the consumer: add `position_history: Vec<u64>` (or count map) maintained in `make_move`/`unmake_move`, and trigger `GameStatus::Drawn { reason: Repetition }` in `refresh_status` when the count hits `rules.draw_policy.repetition_threshold`. See `state/repetition.rs` (currently a stub).

## P2

Worth doing, no rush.

- [ ] **[S] chess-web: in-game rule editor (gear icon → modal)** — Picker-side form already encodes rules into the URL (`?strict=1`, `?house=chain,rush&seed=N`). Add a sidebar gear that opens a pre-filled modal reusing the picker components, navigates to the new query, and starts a fresh game. → [research](backlog/web-ingame-rules-modal.md)
- [ ] **[S] chess-web: WS auto-reconnect with backoff** — PR-1 surfaces "Disconnected — refresh" when the WS read pump closes. Add an exponential-backoff retry that reuses the URL+password and resets the `Hello` flow on success. → [research](backlog/web-ws-reconnect.md)
- [ ] **[S] chess-web: CJK/ASCII glyph toggle in UI** — `glyph::Style` is already plumbed; expose a sidebar toggle + persist in `localStorage`. Shares no logic with chess-tui's `--style` flag.
- [ ] **[M] Promote orient.rs / glyph.rs to a shared client crate** — Both `clients/chess-tui/src/orient.rs` and `clients/chess-web/src/orient.rs` (likewise `glyph.rs`) are byte-identical. Pull into `clients/chess-client-shared` once a third client appears or the copies first diverge. → [research](backlog/promote-client-shared.md)
- [ ] **[S] chess-web: Playwright E2E smoke** — Headless two-tab flow that creates a room, plays a few moves, resigns, and rematches. Cheaper than `wasm-bindgen-test` for full-app smoke. → [research](backlog/web-playwright.md)
- [ ] **[M] chess-net: mixed-variant rooms** — Today one server boots with one `--variant` and every auto-created room copies it. Move to per-room `RuleSet` (a chooser inside the `CreateRoom` form). Architecture supports it (`RoomState` already owns its `GameState`); blocks on UX decisions about a variant filter inside the lobby and what default to apply when joining via a URL with no variant hint.
- [ ] **[S] chess-net: chat moderation primitives** — Current chat is a friend-only space (50-line ring buffer, no rate limiting, no mute, no word filter). Add per-player rate limit (~1 msg/sec leaky bucket), client-side `ChatMute` toggle, and an opt-in `--chat-filter <list>` server flag for substring scrubbing. → [research](backlog/chess-net-chat-moderation.md)
- [ ] **[S] chess-net: chat system messages** — `ChatLine.from` is `Side` today so there's no in-channel narration when seats fill / leave / rematch resets. Widen the type to an enum with a `System` variant and emit join/leave/resign system lines. Wire-breaking → bumps protocol to v4. → [research](backlog/chess-net-system-messages.md)
- [ ] **[S] chess-net: spectator chat permission per room** — ADR-0006 picked players-only as the default; a streamer flow wants a per-room "spectators may chat" toggle (set on room creation, read by `process_chat`'s gate). Small protocol addition next to room password.
- [ ] **[S] chess-net: reconnect-as-same-seat** — Disconnecting frees the seat; the reconnect path currently rebooks at the next free side. Add a one-shot rejoin token (issued in `Hello`, presented as `?token=...` on reconnect) so a flaky link doesn't lose your color.
- [ ] **[S] chess-tui: chat scrollback (PgUp/PgDn)** — The chat pane shows the most recent N lines that fit the sidebar; older lines from the 50-line ring buffer are unreachable. Add scroll keys + a "↑ N more" indicator. Pure UI — no protocol change.
- [ ] **[S] chess-web: chat polish** — Emoji picker, markdown-lite (bold/italic/code), unread-message badge when the chat panel is scrolled up. Cheap UX wins; none touch the protocol.
- [ ] **[S] chess-net: lobby filter + sort** — `/` filter inside the lobby (by id substring or variant); sort by recency / seat count / locked-or-open. Defer until ≥10 concurrent rooms is a real workflow — for a handful of friends the unsorted live list works fine.
- [ ] **[S] chess-net: lobby push debouncing** — `notify_lobby` fires on every seat insertion / removal / status flip; back-to-back churn (rematch storms, reconnect retries) can produce a flurry of identical `Rooms` payloads. Coalesce within ≥250ms windows. Premature today; revisit if observable in the wild.
- [ ] **[S] chess-net: server-side admin / kick / signed token** — Close stuck rooms or force-disconnect a misbehaving client without restarting the whole server. Wants either an admin-only ws path (`/admin/ws?token=…`) or signed JSON tokens accepted by `/ws/<id>`. Folds into a future TLS / auth pass.
- [ ] **[S] WXF notation (相3進5 style)** — Chinese-language traditional xiangqi notation. Pattern lives in crates/chess-core/src/notation/wxf.rs (currently empty). ICCS already implemented; mirror the same encode/decode pair.
- [ ] **[M] Time controls (計時器)** — Per-side clocks: main time + increment, byo-yomi, or sudden death. Belongs in chess-net (server-authoritative tick) or as a thin state wrapper if local-only is enough. Adds GameStatus::Won { reason: Timeout } trigger — the WinReason variant already exists. Out of scope: client-side animation; servers tick authoritatively and broadcast remaining time in PlayerView.
- [ ] **[S] Takeback (悔棋) request/approve protocol** — Local hot-seat undo already works via state.unmake_move() and the chess-cli 'undo' command. This TODO is the multiplayer flow: requester sends Takeback, opponent approves/rejects, server unmakes the last N plies (typically 1 or 2) and broadcasts updated PlayerView. Lives in chess-net once that crate exists.
- [ ] **[S] Handicap mode (讓棋)** — Stronger player removes one or more of their own pieces from the initial setup before play begins (xiangqi tradition: 讓單馬 / 讓雙馬 / 讓單炮 / 讓單車 / 讓雙馬一炮 / 讓雙馬雙炮 / etc). Shape likely: Handicap field on RuleSet (so save/load + replay metadata preserve the choice) consumed by setup::build_xiangqi which skips placing the listed pieces. Xiangqi-only for first cut — banqi's random shuffle complicates the equivalent and can wait until someone asks.
- [ ] **[M] chess-ai v6 — pondering during human's turn** — AI is idle while the human thinks; pre-search the predicted human reply into the v5 TT so the AI's response feels near-instant when the prediction matches. Reuses the hint pump's already-running `chess_ai::analyze` as the predictor (top-1 reply), then spawns a background `analyze` on the post-predicted-reply position. TT entries persist; AI's subsequent search hits warm cache. Opt-in via `?ponder=1` URL flag + picker checkbox (extra CPU drain on mobile). Cancellation via `position_hash` invalidation (same pattern as the hint pump). Target: ≥50 % prediction hit rate → 30-50 % perceived AI-response speedup. → [research](backlog/ai-pondering.md)
- [ ] **[M] WebRTC LAN pairing — Phase 6.5: spectator UI on host page** — Phase 6 (beforeunload guard, peer-disconnect toast, OfflineIndicator LAN states, docs/pwa.md "LAN play" section) shipped 2026-05-14. Remaining: spectator slots — host code path supports up to 4 spectator DCs (`HostRoom::attach_remote_spectator_dc` exists), but no UI to add them. Needs plumbing `Rc<HostRoom>` through `PlayConnected` to `OnlineSidebar`, "Add spectator" button that opens a new offer/answer dance modal (separate PeerConnection per spectator), reuse the existing QR + scanner components. Estimated ~200-300 LoC. → [research](backlog/webrtc-lan-pairing.md), Phase 5.5 spec [research](backlog/webrtc-lan-pairing-qr.md)
- [ ] **[M] chess-net protocol v6 — broadcast win-rate samples + spectator-side eval bar** — Local + TUI win-rate display shipped 2026-05-10 (see Done). Net mode currently can't show the eval bar because (a) chess-net's `PlayerView` doesn't carry per-ply eval samples, and (b) running client-side `chess_ai::analyze` on every `Update` for every spectator wastes CPU and gives different numbers per spectator depending on their machine. v6 should add an optional `Vec<EvalSample>` to `RoomSummary` / `Update`, computed once server-side (the room's host runs analyze, broadcasts cp + win%). Reuses the existing `hints_allowed` flag for permission gating (no new room-level toggle). Wire-breaking → bumps protocol to v6. → [research](backlog/chess-net-evalbar.md)

## P3

Someday / nice-to-have.

- [ ] **[L] chess-engine + chess-ai: search and evaluation** — Alpha-beta + iterative deepening + Zobrist transposition for xiangqi. Banqi needs ISMCTS (information-set MCTS) to handle hidden info — research-heavy and a separate problem from xiangqi search. → [research](backlog/chess-ai-search.md)
- [ ] **[S] chess-ai v5.1 — aspiration windows + TT depth-preferred replacement** — Two follow-up search refinements after v5 ID+TT. Aspiration windows narrow the root window around the previous depth's score (re-search on fail-high/low); recovers most of the alpha-beta savings the full-window-at-root fix gave up. Depth-preferred TT replacement keeps high-depth entries longer than always-replace. Both well-known Stockfish/Crafty patterns. v5 was scoped to "minimum viable ID + TT" so each addition is independently reviewable.
- [ ] **[S] chess-tui: net-mode AI hint panel** — v5 protocol carries `hints_allowed` per room; TUI net mode tracks it and surfaces 🧠 in the sidebar but the actual analysis panel is web-only for now. To match the web UX: reconstruct GameState from PlayerView (move `state::reconstruct_xiangqi_state_for_analysis` from chess-web to chess-core), call `chess_ai::analyze` on every Update, render the existing scored-moves panel + PV overlay in the Net sidebar. The infrastructure exists (NetView already has `last_analysis: Option<AiAnalysis>`); just needs wiring + the move helper.
- [ ] **[S] chess-web: move animations** — CSS transitions on piece translate-translate as a `<g transform>` interpolation. Cheap visual polish; doesn't change protocol. Care needed: the SVG renders pieces by display row/col, so animation needs to track piece identity across renders (key-by-square is wrong for moves).
- [ ] **[S] chess-web: history scrubber + undo via UI** — Engine already has `unmake_move` and `Replay::play_to`. Need a sidebar timeline that replays positions visually without committing to engine state. Reuse `Replay::from_game` so server replays + client scrubbing share one path.
- [ ] **[S] chess-web: mobile layout** — Sidebar collapses below the board on narrow viewports; piece tap target enlarged. Touch input already works via SVG `on:click`; this is purely CSS + viewport meta.
- [ ] **[M] chess-web PWA: persist replays in IndexedDB / OPFS** — Now that local pass-and-play runs offline, the next obvious gap is that closing the tab loses the in-progress 對局. Engine has `Replay::from_game` ready; needs a small IndexedDB wrapper (`chess-web/src/persist.rs`) keyed by variant + start timestamp, plus a "Resume last game" affordance on the picker. When online comes back, optionally sync to a server endpoint (out of scope for v1). When this lands, also include `Vec<EvalSample>` (from the 2026-05-10 win-rate display) in the persisted record so resumed games show the full pre-resumption trend chart, not a chart starting from ply N.
- [ ] **[S] chess-web PWA: Web Share API for FEN/PGN of current position** — The end-game overlay already has share-link copy planned; pair it with `navigator.share({ title, text, url })` so installed PWA users get the native share sheet on mobile (sends to Messages / Line / WeChat etc. without leaving the app).
- [ ] **[S] chess-web PWA: replace placeholder icons** — `clients/chess-web/public/icons/` ships with a generated 帥-on-tile placeholder. Real icon work (proper xiangqi board art / chess piece silhouette in the maskable safe zone) is a designer task. `scripts/regenerate-placeholder-icons.sh` is the rasterize entry point — point it at a redesigned `icon.svg` / `icon-maskable.svg` and re-run.
- [ ] **[S] chess-web: i18n strings table** — All copy is currently inline English+CJK. Pull into a strings module keyed by locale, switch via `?lang=zh-Hant` or browser locale.
- [ ] **[S] chess-web + chess-tui: persisted post-game card with share link** — End-of-game overlay auto-fades after 3 s today; a "save / share this result" affordance would let players link to a replay (engine has `Replay::from_game` ready; need a URL scheme like `/replay/<base64>`). Pure follow-up to the FX overlay shipped 2026-05-07. Web-only first; chess-tui can mirror with a clipboard copy of the same URL.
- [ ] **[S] chess-tui + chess-web: picker redesign (rules/style preview panel)** — Current picker shows a hero block + variant cards; a richer flow would let users live-preview the board with selected rules + glyph style before starting. Out-of-scope of the 2026-05-07 polish PR (kept minimal). Worth doing once the rule-editing modal (`backlog/web-ingame-rules-modal.md`) lands so we can share components.
- [ ] **[S] Replay format (PGN-equivalent)** — Once notation is full (WXF + ICCS + banqi), the history Vec<MoveRecord> already contains everything. Add a serializer to text format + import. Unblocks observers / spectators / training data.
- [ ] **[S] insta snapshot tests for initial positions** — Lock in the visual rendering of fresh xiangqi/banqi positions. Catches regressions in setup.rs that pass type-checks but place pieces wrong. Plan flagged this; deferred from PR 1 because unit tests in setup.rs already cover key invariants.
- [ ] **[S] chess-web: hint button "thinking…" indicator** — Clicking 🧠 Show AI hint kicks off a ~100-300 ms `chess_ai::analyze` search; the panel stays empty until the result lands, with no visual feedback during the gap. Add a spinner / "Analyzing…" line inside the empty panel state (DebugPanel currently shows `<p class="muted">"Waiting for analysis…"</p>` as fallback — upgrade to an animated indicator). Same fix benefits the first-load case in vs-AI mode where the panel mounts before the first analyze completes. Pure CSS animation; no logic change.
- [ ] **[S] chess-ai: cp→win% calibration for xiangqi** — Win-rate display shipped 2026-05-10 uses logistic `1 / (1 + 10^(-cp / WIN_PCT_K))` with `WIN_PCT_K = 400` borrowed from chess Elo convention. The "right" K for xiangqi is unknown — chess and xiangqi have different material balances (no queens, fewer minor pieces, big chariot). Calibrate by running the existing `chess-ai/tests/perf.rs` fixtures over many self-play games at fixed depth, recording `(cp_after_each_move, eventual_winner)` pairs, then fitting K by maximum likelihood. Likely lands K somewhere in 200–600 range. Wrong K just makes the eval bar swing too aggressively or feel sluggish — strictly UX, not correctness. Methodology + result update [`docs/ai/win-rate.md`](docs/ai/win-rate.md) §8 (no new file — fold the calibrated K + N-games + RMSE into the existing doc as historical record).
- [ ] **[S] chess-web + chess-tui: sound effects (move + capture + check)** — Move click, capture thud, 將軍/CHECK chime via the Web Audio API (web) and `bell` ASCII codes / a small bundled WAV (TUI). Wire into existing FX prefs alongside `fx_confetti` and `fx_check_banner` — three new toggles (`fx_sound_move`, `fx_sound_capture`, `fx_sound_check`). Default OFF (audio is the most opinionated polish; users may be in libraries / streams). Source files: short royalty-free CC0 from freesound.org or hand-synthesized via Web Audio oscillator nodes (smaller bundle, less personality). No protocol change; client-only.

## P?

Needs a spike before committing to a real priority. Tag as `[?/Effort]`.

- [ ] **[?/L] Wish/SSH multiplayer alternative to chess-net** — Original analysis (docs/architecture.md) noted Go+wish would give 'ssh chess.you.dev' for free. Rust equivalent: russh (less polished). Spike: is this simpler than chess-net + chess-web for the 'play with friends' scenario, or just neat?
- [ ] **[?/L] Unity renderer via FFI** — Architecture deliberately keeps chess-core platform-agnostic so Unity is reachable. Spike: does building a chess-core C ABI + Unity importer get us anything beyond 'Web with art assets via Leptos + sprites'? Likely not, but documenting the option closes the loop.
- [ ] **[?/M] chess-web: real WASM download progress meter** — Spike a custom bootstrap that fetches the Trunk-generated WASM with byte-level progress, then passes the loaded module/bytes into wasm-bindgen init; only worth doing if release WASM size or remote latency makes the current indeterminate loader insufficient. → [research](backlog/web-wasm-download-progress.md)
- [ ] **[?/S] Pin rust-toolchain.toml to match CI** — CI uses `dtolnay/rust-toolchain@stable` (auto-bumps to latest stable). Local toolchains drift, so pushes that pass `cargo clippy -- -D warnings` locally periodically fail in CI when a new clippy lint lands (recurred 2026-05-08, three round-trips). Pinning fixes the drift but turns the implicit "free updates" into a manual chore — defer until a second contributor or a third recurrence. → [research](backlog/rust-toolchain-pin.md), see also [pitfalls/clippy-version-drift-local-vs-ci.md](pitfalls/clippy-version-drift-local-vs-ci.md)
- [ ] **[?/L] chess-ai: opening book** — Pre-computed table for the first ~10 plies indexed by `position_hash` (v5 Zobrist field). Would meaningfully improve early-game play at zero search cost — every difficulty starts with "professional" first moves. Open questions before commit: (1) data source (open-licensed db, public games, or hand-curated 500-1500 entries?), (2) variation handling (deterministic vs weighted-random — latter doubles data + complicates determinism for tests), (3) WASM bundle size (10k entries ≈ 160 KB, 100k ≈ 1.6 MB), (4) staleness without a living curator. Spike scope: one afternoon to survey sources + estimate coverage + prototype `BookEntry` format. → [research](backlog/ai-opening-book.md)

## Done

- ✅ [2026-05-14] [P2/S] WebRTC LAN pairing — Phase 6 polish — Four UX-safety items on top of Phase 5 + 5.5: **(1)** `beforeunload.rs::use_beforeunload_guard` — install/remove a `beforeunload` listener as a `Signal<bool>` flips; wired into `LanHostPage` + `LanJoinPage` so closing the tab during `Playing` shows a confirmation prompt. Includes `on_cleanup` belt-and-suspenders so the listener never outlives its component. Modern browsers ignore the message text and show a generic prompt — passed for older-browser compat. **(2)** Peer-disconnect toast — host side via `host_room.rs::drop_peer` pushing a synthetic `ServerMsg::Error { message: "Peer disconnected — LAN game ended." }` onto the host's local sink (the play page's existing toast pump surfaces it without any new code path); joiner side via a new effect in `pages/play.rs::PlayConnected` that watches `conn` for Open → Closed/Error transitions and sets the toast. Tracks previous state via `Rc<Cell<ConnState>>` so the effect only fires on the transition (not every render). **(3)** `OfflineIndicator` LAN states — new `LanIndicator { Idle, HostWaiting, JoinerWaiting, Connected, Disconnected }` enum + `lan: RwSignal<LanIndicator>` field on `PwaState`. `Idle` (the off-LAN-pages default) preserves the old plain online/offline behavior unchanged. LAN pages set the indicator via a `create_effect` derived from their `status` signal + `on_cleanup` reset. Joiner page also watches the inner `state_sig` for Open → Closed transitions to switch to `Disconnected`. New CSS modifier classes with appropriate colors (blue connected, amber pulsing waiting, red dropped); pulse animation gated by `prefers-reduced-motion: reduce`. **(4)** `docs/pwa.md` "LAN play" section — explains the HTTPS install prereq → unlocks `getUserMedia` + `RtcPeerConnection` + service-worker-registration triumvirate that LAN play needs, indicator behavior table, and how the SW handles `/lan/host` + `/lan/join` SPA routes plus the auto-precached `jsQR.min.js`. Skipped from this phase: spectator UI on host page (deferred to Phase 6.5 since it requires plumbing `Rc<HostRoom>` through `PlayConnected.injected_session` to the sidebar, ~200-300 LoC). Skipped sub-item: host-side `OfflineIndicator → Disconnected` transition — host's `Session.state` is hardcoded `Open` (it's the local sink, never closes), and `HostRoom.drop_peer` doesn't have a clean callback hook to PwaState; toast covers the user-facing notification so this is acceptable until the spectator UI brings the wider `HostRoom`-to-page plumbing along.

- ✅ [2026-05-13] [P2/M] WebRTC LAN pairing — Phase 5.5 (QR + camera scan) — Six-commit slice on top of the Phase 0-5 textarea-only base. **C1**: `qrcode = "0.14"` crate dep + `components/qr.rs::QrCodeView` Leptos component (ECC level M, auto-version, ~315 px SVG for typical ~660-byte SDP); rendered side-by-side with the existing textarea on both `/lan/host` (offer) and `/lan/join` (answer). **C2**: `public/jsQR.min.js` (130 KB minified) + `qr_decode.rs` Rust interop wrapper with `decode_rgba(data, w, h)`, `is_available()`, and `ensure_loaded()` for lazy script injection (uses `window.location.origin + "/jsQR.min.js"` to sidestep the SPA relative-URL trap where `/lan/host` would resolve `jsQR.min.js` to `/lan/jsQR.min.js` = SPA fallback HTML). **C3**: `camera.rs` with `has_camera()` (enumerateDevices), `camera_permission()` (Permissions API → `Granted`/`Denied`/`Prompt`/`Unknown`), `open_camera_stream()` (`getUserMedia({video: {facingMode: 'environment'}})`). **C4**: `components/qr_scanner.rs::QrScanner` modal — full-screen overlay with `<video>` + offscreen `<canvas>` + self-rescheduling `requestAnimationFrame` decode loop. Camera tracks stopped on teardown so the browser camera-active red-dot doesn't leak. Self-rescheduling closure pattern via `Rc<RefCell<Option<Closure>>>` + separate `Rc<Cell<bool>>` stop-flag. Wired into both LAN pages with `cam_available` gating + auto-fire next-step on decode. **C5**: `ScanStatus::PermissionDenied` variant with platform-aware hint text (iOS Settings → Safari → Camera, Android Chrome lock-icon → Permissions, Desktop site settings) plus a reminder that pasting text always works without the camera. Pre-flight permission check + post-error string-match for browsers without `navigator.permissions` for camera. ARIA polish: `aria-modal`, `tabindex`, Escape-key cancel, 44×44 px Cancel button (WCAG 2.5.5). **C6**: `scripts/test-lan-pairing.sh` — playwright-cli E2E that drives the textarea path (camera path is real-device only since headless Chrome has no usable webcam) — verifies SDP exchange + QR rendering on both sides + chat sync. **PC-without-webcam constraint preserved**: textarea + Copy + Bytes counter is a co-equal alternative on every screen, never a fallback. Camera button hidden when `has_camera == false`. Out of scope for v1: Web NFC tap-to-pair (Android-only), torch toggle (Android-only), pure-Rust `rqrr` decoder (deferred until decode latency proves an issue), spectator UI (Phase 6).

- ✅ [2026-05-13] [P2/L] WebRTC LAN pairing v1 — Phase 0-5 of `backlog/webrtc-lan-pairing.md` shipped. Two browsers / phones on the same LAN play 1v1 xiangqi over `RtcDataChannel` with NO chess-net server in the loop, NO external network at runtime. **Phase 1**: extracted `chess-net::room::Room` — transport-agnostic state machine (`Room::apply(peer, ClientMsg) -> Vec<Outbound>`); `server.rs` becomes a thin WS wrapper; 17 new unit tests + all 50+ existing WS integration tests still green. **Phase 2**: `transport::Transport` trait + `Session { handle, incoming, state }` shape in chess-web; existing `ws.rs` becomes one impl. **Phase 3**: `transport::webrtc` — `connect_as_joiner` / `connect_as_host` with reliable+ordered DataChannel, JSON SDP envelope (CRLF-normalised for AirDrop/IM safety), `IceMode { LanOnly, WithStun }`, CN-friendly STUN list (miwifi.com + qq.com + cloudflare + google), 5 s ICE-gather timeout. **Phase 4**: `host_room::HostRoom` — host PWA owns the `Room`; multi-peer routing via `HashMap<PeerId, PeerSink>` (Local for host's own play page, Remote for joiner's DC, Mock for tests); `Weak<HostRoom>` in DC closures to break the Rc cycle. **Phase 5**: `/lan/host` + `/lan/join` route components with textareas + Copy buttons (QR deferred to follow-up); `pages::play::PlayConnected` gains `injected_session` + `back_link_override` props so the same play UI serves chess-net + LAN modes unchanged. End-to-end verified via `playwright-cli` two-tab automation: host opens room → joiner pastes offer → generates answer → host pastes + accepts → both flip to play view → host moves cannon h2→h4 → joiner sees `Black 黑 to move` and the cannon position update at the synced square. Three Leptos+WebRTC pitfalls discovered and documented during testing: `pitfalls/leptos-rwsignal-queue-self-clear-race.md`, `pitfalls/leptos-create-effect-inside-spawn-local-silent-gc.md`, `pitfalls/webrtc-set-remote-description-resolves-before-dc-open.md`. Spike pitfall: `pitfalls/webrtc-mdns-lan-ap-isolation.md` (Mi AX9000 silently drops WebRTC `<uuid>.local` resolution). Wire protocol unchanged from chess-net v6. Follow-up Phase 5.5+6 (QR generation/scan, spectator slots, polish) parked at P2 above.

- ✅ [2026-05-10] [P2/M] AI win-rate display — eval bar + sidebar badge + per-ply samples + post-game trend chart (web local + chess-tui) — User asked "覺可以有 AI real-time 判斷場面與雙方勝率的功能? ... 然後可以在下棋 history 中也記錄每個 snapshot 的勝率 最後結束時就可以畫一個勝率走勢圖". Shipped end-to-end: **chess-ai** gains `cp_to_win_pct(cp: i32) -> f32` + `WIN_PCT_K = 400` (logistic with `[0.01, 0.99]` clamp; 6 unit tests covering monotonicity, mate-clamp, KING_VALUE-clamp, range invariants). **chess-web** gains `?evalbar=1` URL flag + picker checkbox "📊 Win-rate display"; new `eval.rs` module with `EvalSample { ply, side_to_move_at_pos, cp_stm_pov, red_win_pct }` (red-POV normalised at sample-write time); two new SVG components — `<EvalBar>` (vertical strip attached to the right edge of the board inside a new `.board-pane__row` flex wrapper) and `<EvalChart>` (260×140 viewBox polyline with per-sample hover tooltips, filled advantage area, 50 % reference line); sidebar gets a `紅 % • 黑 %` badge (`<EvalBadge>`); `<MoveHistory>` rows get optional `+5%` / `-12%` mover-POV delta annotations. Sample-write effect watches both analysis caches + history.len() and pushes idempotently per-ply. Hint pump's gate widens to `if hints || evalbar` so PvP+evalbar still gets samples without the `?hints=1` flag. **chess-tui** gains `--evalbar` CLI flag + `eval.rs` mirror struct + `Action::EvalbarToggle` (`E` key); `apply_move` runs a synchronous `chess_ai::analyze` (~10–300 ms native) in PvP+evalbar mode, vs-AI gets samples free from `ai_reply`'s existing analysis output. Sidebar shows a one-line `Eval: 紅 ████████░░░░ 黑 (52%)` headline whenever evalbar is on; press `E` to also show a 30-char ASCII trend chart (one row per ply, last 24 plies). Net mode + banqi/three-kingdom **out of scope** — see follow-ups: P2/M `chess-net protocol v6 — broadcast win-rate samples` (server-side analyze + spectator-side display) → [research](backlog/chess-net-evalbar.md), P3/S `chess-ai: cp→win% calibration for xiangqi` (K=400 borrowed from chess Elo, may need tuning), and PWA persistence TODO updated to bundle EvalSample with replay records when that lands. Workspace `cargo fmt --check` + `cargo clippy --workspace --all-targets -- -D warnings` + `cargo test --workspace` (all 38 chess-ai + 104 chess-core + 83 chess-web + 32 chess-net tests + 4 routes round-trip tests for the new flag) + `cargo check --target wasm32-unknown-unknown -p chess-web` all green. Full design record: [`backlog/ai-winrate-display.md`](backlog/ai-winrate-display.md).

- ✅ [2026-05-10] [P3/L] Threat highlight (Display setting) — web + TUI + chess-core protocol v6 — User asked for a checkbox to highlight 被叫殺 pieces, but worried it'd be visually busy because mid-game often has many pieces simultaneously attackable. Shipped a 4-mode dropdown (Off / Attacked / **Net loss** default / Mate threat) plus an orthogonal "Hover preview" toggle, all behind one Display-settings UI. **Engine** (`crates/chess-core/src/eval/see.rs` + `rules/{xiangqi,banqi}.rs`): new `eval` module with a fixed-point piece-value table (General=10000, Chariot=9, Cannon=5, Horse=4, Advisor/Elephant=2, Soldier=1 with past-river bump to 2) and a variant-aware Static Exchange Evaluation. Three threat-detection helpers per variant (`attacked_pieces`, `net_loss_pieces`, `mate_threat_pieces`) dispatched via `GameState`. Banqi attack enumeration excludes hidden pieces (face-down attackers carry no public-information threat — see backlog rationale). Cannon defenders in banqi are conservatively flagged regardless of SEE because the cannon-only-via-jump asymmetry leaves standard SEE blind to a hung cannon. **Protocol v6** adds `PlayerView.threats: ThreatInfo { attacked, net_loss, mate_threats }` with `#[serde(default)]` so v5 clients silently degrade to "Off mode". **chess-web**: `Prefs.fx_threat_mode` / `fx_threat_hover` persisted to localStorage; bilingual `<select>` + checkbox in Display settings; new `ThreatOverlay` Board prop renders red rings (`.threat-mark`), magenta dashed for mate (`.threat-mark--mate`), soft-red dashed for hover (`.threat-mark--hover`). Hover-preview computes a what-if delta (`hover_threat_squares`): clone the state, remove the selected piece, set-difference the post-removal `attacked_pieces` against the pre-removal baseline — answers "what is this piece defending?". **chess-tui**: `--threat-mode={off,attacked,net-loss,mate-threat}` + `--threat-on-select` CLI flags; cell-bg tinting via `ThreatSets` plumbing through `intersection_or_piece` (mirrors the existing `debug_from`/`debug_to` pattern). Tints applied BEFORE selection / cursor so user-driven highlights stay dominant. Both clients work for xiangqi + banqi. **Tests**: 5 SEE (no-attacker / free-general / equal-trade / soldier-takes-chariot / past-river-bump), 5 xiangqi threat (free chariot, defended chariot SEE-zero, in-check general excluded, mate-threat fires in 3-chariot fixture, mate-threat empty in opening), 2 banqi (hidden attacker ignored, revealed attacker flagged), 3 view (round-trip, populated lists, pre-v6 backward-compat), 2 web hover-preview. All 119 chess-core + 74 chess-web + 32 chess-tui + clippy + wasm32 green. Full design + decisions worth re-litigating: [`backlog/threat-highlight-feature.md`](backlog/threat-highlight-feature.md).

- ✅ [2026-05-10] [P3/M] chess-ai: fix silent depth-setting truncation + UX restructure (Custom-difficulty combo) — User reported setting Search depth = 10 in the Xiangqi vs Computer picker but the AI Debug panel showed Depth = 4. Investigation found the value was honored end-to-end (URL → AiOptions → engine) but v5's iterative deepening hit the flat 250 k `NODE_BUDGET` partway into depth 5, returned the last completed depth (4), and the panel honestly displayed it — no indication the user's request was clipped. Three-stage fix shipped: **(1)** Surface the truncation: new `target_depth: u8` + `budget_hit: bool` fields on `AiAnalysis`; web debug panel renders "Depth: 4 / 10 (cap)" with tooltip when budget hit, TUI debug header mirrors the format. **(2)** Engine policy fix: replaced flat `NODE_BUDGET` for v5 with `node_budget_for_depth(target)` — baseline 250 k for `target ≤ 4` (preserves Hard's documented perf), then doubling per extra ply (5→500k, 6→1M, …, 9→8M), capped at `MAX_AUTO_NODE_BUDGET = 16 M` for `target ≥ 10` (~5–10 s WASM ceiling). v1–v4 keep flat constant (they don't iterate). New regression `higher_target_depth_reaches_strictly_deeper_in_opening` proves depth=8 now reaches strictly deeper than depth=4 from busy openings. **(3)** Picker UX restructure (per user feedback): replaced the standalone "Search depth (advanced)" fieldset with a 4th "Custom (advanced)" radio inside the existing Difficulty group; selecting it reveals both Search depth + Node budget inputs together (they control the same parameter — depth-via-difficulty-preset and depth-via-explicit-input were artificially separate). Custom rides on top of `Difficulty::Hard` so the Variation fieldset's "Default" still resolves to a power-user-appropriate `Randomness::SUBTLE`. New `AiOptions.node_budget: Option<u32>` field, web URL token `&budget=N` (clamped 1..=`MAX_AI_NODE_BUDGET` = 64 M), TUI `--ai-budget N` flag. Class-of-bug doc at `pitfalls/ai-search-depth-setting-shows-depth-4.md` (symptom, root cause, prevention guidance for future engine versions). Updated tests: 51+ chess-ai (5 new for the budget policy, 2 new for the AiAnalysis schema), 6 new chess-web routes round-trip tests for `&budget=`. All clippy + wasm32 + workspace tests green.

- ✅ [2026-05-10] [P3/S] chess-web PWA — installable, offline-capable, version-aware — chess-web becomes a real PWA on top of the existing dual-target Trunk build (chess-net `--static-dir` + GitHub Pages subpath). New: `public/{sw.js.tmpl, manifest.webmanifest.tmpl, pwa.js, icons/}`; `scripts/build-pwa.sh` post-build hook (Trunk `[[hooks]]`) scans `dist/` for the hashed JS/WASM/CSS Trunk just emitted, computes a content-hash `APP_VERSION`, substitutes `__BASE_PATH__`/`__APP_VERSION__`/`__PRECACHE_MANIFEST__` into the templates, drops `dist/{sw.js, manifest.webmanifest}` ready to ship — same hook serves server (`/`) and GitHub Pages (`/Chinese-Chess_Xiangqi/`) modes by reading `TRUNK_PUBLIC_URL`. SW strategies: navigation network-first → SPA shell fallback (offline route changes still work via Leptos router), hashed assets cache-first, `/ws*`/`/lobby`/`/rooms`/`/api/*` bypassed. Update UX is opt-in: a fresh build precaches but the old SW keeps controlling the page until the user clicks the Reload toast (skipWaiting → controllerchange → reload) — chosen so an in-progress 對局 isn't reloaded out from under the player. UI: `clients/chess-web/src/components/pwa.rs` (`PwaState` hydrated once at app boot, `PwaInstallBanner` on the picker, `PwaInstallButton` in the sidebar footer, `PwaUpdateToast` global, `OfflineIndicator` bottom-right). iOS Safari (no `beforeinstallprompt`) gets a manual "Add to Home Screen" hint variant. Placeholder icons rendered from committed SVGs by `scripts/regenerate-placeholder-icons.sh`. Full takeaway: `docs/pwa.md`; common traps: `pitfalls/pwa-base-path-and-stale-cache.md`.

- ✅ [2026-05-10] [P3/M] Hint mode UX overhaul: button-toggled debug↔hint, PvP-emit fix, board coordinates, picker mode-aware checkboxes — Three issues raised by user testing the previous hint-mode commits. **(1) PvP+`?hints=1` from picker showed no panel.** Root cause: `routes.rs::build_local_query` had `hints=1` emission inside the `if mode == VsAi` block (alongside `debug=1`, `engine=`, etc.), so the picker's PvP-mode + checked hints box silently dropped the URL flag. Fix: move `hints=1` emission out of the VsAi block (kept `debug=1` inside since debug is meaningless in PvP — no AI move pump to fill the cache). Added two regression tests (`xiangqi_pvp_hints_emitted_even_without_ai_mode`, `xiangqi_pvp_hints_round_trips`). **(2) Both-mode (`?debug=1&hints=1`) clobbered debug content.** AI pump and hint pump shared one `ai_analysis` signal; AI move triggered the hint pump ~100ms later, overwriting the AI's POV cache before the user could read it ("AI下棋很快, 內容稍縱即逝"). Fix: split into two distinct caches — `debug_analysis` (AI pump writes only) + `hint_analysis` (hint pump writes only). Panel reads via `Signal::derive` toggling on `hint_view_active` (the sidebar button). New behavior: button OFF in both-mode → shows debug content (sticky); button ON → shows hint content (live). User can flip back and forth without losing either. Hint pump now runs whenever hints enabled (drops the previous `&& !debug_enabled` gate) so cache stays warm. Title reflects current view ("🧠 AI Hint" or "🔍 AI Debug"), not flag combination — drops the misleading "🔍 + 🧠" combined label. **(3) Picker debug checkbox visible in PvP.** PvP has no AI to debug, so the checkbox doesn't apply. Wrapped in `<Show when=mode == VsAi>` — PvP picker shows only "🧠 Allow AI hints"; vs-Computer picker shows both. **(4) Board lacked coordinate labels.** Hint table moves like "h2g2" had no on-board reference. Added `.coord-layer` to the SVG board with file labels (a-i) on top + bottom edges and rank labels (0-9) on left + right edges, observer-aware (Red sees a..i left-to-right; Black sees i..a). New `.board-coord` CSS (faded monospace, 11px, opacity 0.55). Net mode (`play.rs`) got the title cleanup too — single pump there means the "+" combined label was always wrong; now uses `🧠 AI Hint` when hints requested else `🔍 AI Debug`. All 30 chess-ai + 89 chess-core + 60 chess-web + 32 chess-net + clippy + wasm32 green; 2 new regression tests for the PvP-hints emit fix.

- ✅ [2026-05-10] [P3/S] Hint mode: dynamic panel title + PvP support — User feedback on the previous decoupling commit identified two more issues. **(1) Panel title was always "AI Debug 🔍" regardless of which mode was active.** Confusing in `?hints=1`-only mode where the user is asking for help, not debugging. Fixed: `<DebugPanel>` now takes `title: Signal<String>` and `subtitle: Signal<String>` props. `pages/local.rs` and `pages/play.rs` derive: "🧠 AI Hint" / "🔍 AI Debug" / "🔍 AI Debug + 🧠 Hint" based on which flags are active. Subtitle shows current side-to-move ("Red 紅 to move" / "Black 黑 to move") so the user knows whose POV the analysis is from. **(2) Pass-and-play (PvP, no AI) couldn't access the hint panel.** The hint pump was gated on `if let Some(cfg) = ai`, and the picker hid the AI insight checkbox inside the `<Show when=VsAi>` block. Refactored: split `VsAiConfig` (AI opponent knobs) from new `InsightConfig { debug, hints }` (panel knobs); hint pump is now driven by `InsightConfig` not `VsAiConfig`. For PvP, hint search uses sensible defaults (Hard difficulty, default Strategy v5, STRICT randomness). Picker fieldset moved outside the VsAi-only Show so both modes get the checkboxes. The hint button now appears in the sidebar for PvP+`?hints=1` so two humans sharing a device can ask the bot for advice. All 30 chess-ai + 89 chess-core + 58 chess-web + 32 chess-net + clippy + wasm32 green.

- ✅ [2026-05-10] [P3/S] Hint mode: separate Debug + Hint picker checkboxes + fix POV clobber bug — Two follow-up fixes after the user tested the previous decoupling commit. **(1) Picker now has TWO independent checkboxes** instead of one: "🧠 Allow AI hints" (writes `?hints=1`, on-demand toggle button, default hidden) and "🔍 AI debug panel" (writes `?debug=1`, sticky always-on panel for development). Both can be enabled together; user wanted to enable each independently. Fieldset legend renamed "AI insight panels (advanced)". **(2) Bug fix**: in `?hints=1` mode (without `?debug=1`), the hint table was getting clobbered with AI-POV data right after each AI move, so only the very first hint showed correctly and subsequent ones looked like debug output. Two compounding causes: (a) AI pump was caching its own POV `ai_analysis` on `cfg.debug || cfg.hints` (should have been just `cfg.debug`), (b) hint pump bailed out on epoch mismatch — `state.update(...)` triggers the hint pump's reactive effect BEFORE `move_epoch.update(...)` lands, so the captured `cur_epoch` was stale by the time the spawned task awaited 40ms and re-checked. Both fixed: AI pump now only caches when `cfg.debug`; hint pump uses `position_hash` (from chess-core v5 Zobrist) for staleness check instead of move_epoch, and tracks `ai_thinking` reactively so it re-fires when the AI finishes (state.update fires effect with thinking=true → bail; ai_thinking.set(false) fires effect again → runs). All 30 chess-ai + 89 chess-core + 58 chess-web + 32 chess-net + clippy + wasm32 green.

- ✅ [2026-05-10] [P3/S] Hint mode UX: decouple from debug mode (toggle button + on-demand player POV) — Follow-up to the earlier hint-mode ship after user feedback. Original implementation had `?hints=1` aliased to `?debug=1` (always-on panel showing AI's POV), but the user clarified the semantic split: **debug** = post-AI analysis ("why did the bot pick this?", sticky panel), **hints** = pre-user analysis ("if the bot were me, what would it play?", on-demand toggle, default hidden). Now properly separated. Sidebar gains an optional `hint_toggle: Option<RwSignal<bool>>` prop (local) / `show_hint_button: Signal<bool> + hint_open: RwSignal<bool>` (net) — when present, renders a "🧠 Show / Hide AI hint" button. Default closed; click to expand/collapse mid-game. New `hint_pump` effect in `pages/local.rs` runs `chess_ai::analyze` from the **human's** POV whenever the toggle is open AND it's the human's turn AND game is ongoing AND AI isn't mid-think (skips when AI pump is doing its own analysis). Net mode (`pages/play.rs`) reuses the same effect, gated on server's `hints_allowed` (still server-permission-required for fairness). Both modes use the same `<DebugPanel>` mount but `panel_visible = debug || (hints && hints_open)`. Picker checkbox label updated: "Allow AI hints — adds a 🧠 toggle button in the sidebar during play." (was: "Show AI hints — open a side panel..."). New `.btn-hint` / `.btn-hint--on` CSS (muted blue, full-width sidebar button). All 30 chess-ai + 89 chess-core + 58 chess-web + 32 chess-net tests + clippy + wasm32 green.

- ✅ [2026-05-10] [P3/M] Hint mode (web + TUI + chess-net protocol v5) — Anti-cheat-friendly opt-in for the AI debug/analysis panel in net play. **Local mode (incl. GitHub Pages standalone)** keeps `?debug=1` and the new `?hints=1` URL flags working freely — no server, no permission needed; both flags are functionally equivalent and trigger the same DebugPanel mount. Picker checkbox renamed "Show AI hints" and writes the canonical `?hints=1` URL token. **Net mode** gates the panel on the room's server-side `hints_allowed` flag (PROTOCOL_VERSION 4→5, additive `#[serde(default)]`): first joiner's `?hints=1` query param (or web lobby's "🧠 Allow AI hints" checkbox) sets `RoomState.hints_allowed = true` for the room's lifetime, frozen like password. Server echoes the flag in `Hello`, `Spectating`, and `RoomSummary`. Closes the previous client-only `?debug=1` cheat hole — net mode now refuses to mount the panel unless the room sanctioned it (toast warns the user). Banner "🧠 AI hints enabled in this room" mounts when allowed; lobby shows 🧠 badge next to 🔒 password badge. **TUI**: `--hints` CLI flag on root command (works with `--connect` and `--lobby`), threads through to URL; sidebar shows "AI hints: enabled (🧠)" / "disabled" once welcome arrives; `D` key in net mode shows context-aware message ("waiting for welcome" / "this room has hints disabled" / "use the web client for the full panel" since TUI net analysis UI is deferred — see new P3 TODO). All 30 chess-ai + 89 chess-core + 58 chess-web tests + 32 chess-net tests + clippy + wasm32 green.

- ✅ [2026-05-10] [P3/L] chess-ai v5: iterative deepening + Zobrist transposition table — Two big wins ship together. **(1) Zobrist hashing** lands in chess-core (`state/zobrist.rs`): compile-time SplitMix64-seeded table (~21 KB static, deterministic across native/WASM), new `GameState.position_hash: u64` field with `#[serde(default)]` + `recompute_position_hash()` for back-compat snapshot loads, incremental updates wired into `apply_inner`/`unapply_inner`/side-toggle for all move kinds (Step/Capture/CannonJump/ChainCapture/DarkCapture). 11 new tests cover round-trip-after-make/unmake, 20-ply incremental matches full-recompute, 44 distinct opening hashes for 44 opening moves. Naturally unblocks the P1 threefold-rep TODO (no longer "needs Zobrist", just consumes it). **(2) v5 search** (`crates/chess-ai/src/search/{tt.rs,v5.rs}`): TranspositionTable with always-replace policy + 2^17 = 131k slots (~5 MB, WASM-safe); `negamax_v5` does TT probe at entry, TT-best-move ordering bonus, mate-corrected score storage; `score_root_moves_v5` runs **iterative deepening** d=1..target reusing TT across iterations, returns last-completed depth (may be < target when 250k node budget hits mid-iteration). Default engine flipped to `Strategy::IterativeDeepeningTtV5`. Endgame Hard: 35k nodes/21ms vs v4's 65k/34ms (-46% nodes, -38% wall-clock). Opening Hard: hits node budget at depth 3 (vs v4's truncated d4) — depth lower but quality strictly better (every root move scored consistently). Easy mode +2ms TT alloc overhead, imperceptible. v5 picker label "(recommended)"; aliases `v5`/`id-tt`/`iterative-deepening`/`iterative-deepening-tt` parsed in URL. Full design: `docs/ai/v5-id-tt.md`. Updated perf table: `docs/ai/perf.md`. README + Strategy::ALL list `[v5, v4, v3, v2, v1]` newest-first. 41 chess-ai tests + 89 chess-core tests + 15 chess-web tests + clippy + wasm32 all green.

- ✅ [2026-05-09] [P3/L] chess-ai: Principal Variation + TUI debug mode + Net debug — Three follow-ups to the AI debug panel from earlier. (1) **PV (principal variation)**: `negamax`/`negamax_qmvv`/`score_root_moves[_qmvv]` now return `(score, Vec<Move>)` tracking the best-line continuation per root move. `ScoredMove` gains `pv: Vec<Move>` field. v4's quiescence still returns empty PV (its captures are exploratory, not committed line). Web `<DebugPanel>` shows PV inline next to each move (truncated to 3 plies in table, full chain on board); board's `highlighted_move` prop replaced by `highlighted_pv: Signal<Vec<Move>>` rendering a chain of from→to arrows fading from bright (chosen) to dim (deepest predicted move). Distinct CSS classes (orange for chosen, blue for PV continuation). New test `analyze_chosen_move_has_pv_at_depth_2_and_above` asserts PV is non-empty + chains correctly. (2) **TUI debug mode**: new `D` key toggles `debug_open` panel in sidebar showing scored root moves with `,`/`.` cursor navigation. Cursor row's PV chain is highlighted on the board via bg-color tints (yellow=from, blue=to). `last_analysis: Option<AiAnalysis>` field added to `GameView`; `ai_reply` always calls `analyze` (same cost as `choose_move`). HELP_LINES + hint string updated. Cleared on undo. (3) **Net mode debug**: `?debug=1` URL param on `/play/<room>` runs `chess_ai::analyze` client-side on every PlayerView update. New `state::reconstruct_xiangqi_state_for_analysis(view) -> Option<GameState>` helper rebuilds a search-compatible GameState from PlayerView (xiangqi only — banqi/three-kingdom return None due to hidden info). Pure client-side, no protocol changes; both players and spectators can use it. Mounts the same `<DebugPanel>` component as local mode. PlayConnected gains a `debug_enabled: bool` prop. 30 chess-ai tests, 55 chess-web tests, all clippy + wasm32 green.

- ✅ [2026-05-09] [P3/M] chess-ai: AI debug panel (web) — show all scored root moves + hover-to-highlight on board — User asked for a way to inspect the AI's search results so they can verify the engine's reasoning. New `chess_ai::analyze(state, opts) -> Option<AiAnalysis>` returns the full scored root-move list + metadata (depth, nodes, strategy, randomness); `choose_move` is now a thin wrapper that extracts `chosen`. New web `<DebugPanel>` component (gated by `?debug=1` URL param + picker checkbox) renders a scrollable table of scored moves (best-first, color-coded green/red for good/blunder, ★ on chosen) with metadata at top. Hover any row → board renders dashed `from→to` overlay (yellow rings on origin/destination + dashed connector) so the user can see what the AI was considering. Cleared on undo / new game so panel doesn't show stale info. Routes parse/build round-trips `&debug=1` (truthy aliases: `1`/`true`/`on`); 4 new tests. Board component gains optional `highlighted_move: Signal<Option<Move>>` prop; defaults to "always None" so existing call sites unchanged. Net mode unaffected (PlayerView doesn't carry analysis). No PV (principal variation) yet — search returns one score per root move but doesn't track best-line; PV is a future iteration. TUI debug mode also deferred — web first to validate the UX. 29 chess-ai tests + 55 chess-web tests + clippy/wasm32 all green.

- ✅ [2026-05-09] [P3/S] chess-ai: fix alpha-beta-at-root score pollution + UI placeholder — User reported v4 still picks suicide moves (image 1: AI=Black plays 馬 g9i7 leaving Red free to cannon-jump-mate the General). Investigation: a regression test repro'd the bug across all (difficulty, strategy, seed) combos at Normal/Hard. Root cause was NOT a v4 issue but a long-standing bug in `score_root_moves[_qmvv]`: alpha was narrowed across root moves (standard alpha-beta), causing fail-high in inner search to clamp non-best move scores to the running alpha. With Hard SUBTLE randomness ("top-3 within ±20 cp"), genuine defensive moves (true score -132) and lying suicide moves (clamped to -132 instead of true -50_000) couldn't be distinguished — RNG would pick suicide ~⅓ of the time. v4 surfaced the bug because MVV-LVA orders king-capture FIRST in inner search (highest priority), triggering fail-high earlier; v3's flat capture-first ordering happened to expose suicides via a different path so v3 looked OK. Fix: use full window (`-MATE-1, MATE+1`) for every root move; no inter-root alpha narrowing. Cost: v4 Hard opening jumps from ~30ms (lying) to ~330ms native / 1-3s WASM (correct, hits 250k node budget). All other versions also slower (v3 Hard ~190ms, v1/v2 ~160-190ms). Trade is worth it — correctness > speed; v5 (ID+TT) and PVS-at-root are the future speed recovery paths. Class-of-bug doc at `pitfalls/alpha-beta-root-score-pollution.md`. Regression test `v4_defends_general_after_red_central_cannon_lands_at_e6` mirrors the user's exact game and asserts Normal/Hard never pick a move that leaves the General capturable in 1 ply (Easy can — depth 1 is intentionally weak). Also fixed: Search depth input placeholder "(default)" was clipped to "(de" in narrow input — switched to "auto" + added `.depth-input { min-width: 6rem }` CSS so 2-digit values display fully.

- ✅ [2026-05-09] [P3/M] chess-ai v4 (quiescence + MVV-LVA) + user depth knob — Two ships in one push. **(1) User depth knob**: `&depth=N` URL param (clamp 1..=10) + picker number input + TUI `--ai-depth N` flag (clamp 1..=12). Lets users explore deeper search than the difficulty defaults (Easy=1, Normal=3, Hard=4) for stress-testing or repro-pinning. Plumbs to existing `AiOptions::max_depth` — pure UI work, no library change. **(2) v4 = `Strategy::QuiescenceMvvLvaV4`**: same v3 evaluator, but search now uses MVV-LVA capture ordering (`search/ordering.rs`) and a quiescence search at the horizon (`search/quiescence.rs`, capped 12 plies). Fixes the horizon-effect blunder class — "AI wins a chariot then loses it back next move" — that v3 still leaked at depth 1-3 and occasionally at depth 4. v3-style search code untouched (`negamax_qmvv` lives next to `negamax`); strict superset of v3 strength. New regression test `v4_avoids_horizon_effect_recapture_at_depth_1` proves v4 declines a "free" capture that v3 takes. **Performance**: v4 is ~9-11× more expensive than v3 in busy openings (177k nodes / 280 ms native = ~71% of 250k budget; 1-3s WASM est.); midgame ~2× (33ms); endgame parity (~12ms). v4 + `Difficulty::Normal` + `?depth=3` keeps quality at v3-cost when busy openings feel slow. Roadmap reordered: v5 = ID + Zobrist TT (next, also unblocks P1 threefold-rep), v6 = Web Worker, v7 = ISMCTS for banqi, v8 = optional Pikafish backend. Picker dropdown extended (4 engines), TUI `--ai-engine v4` flag, full docs at `docs/ai/v4-quiescence-mvv-lva.md`. README + perf.md + backlog all updated.

- ✅ [2026-05-09] [P3/S] chess-ai: Randomness presets + perf bench + Hard now varies by default — Refactored the per-difficulty move-pick policy into a unified `Randomness { top_k, cp_window }` struct with named presets (`STRICT`/`SUBTLE`/`VARIED`/`CHAOTIC`). `Difficulty::default_randomness` returns the per-level recommendation; `AiOptions::randomness: Option<Randomness>` overrides. Old per-difficulty match arms in `engines::run` collapsed into a single `pick_with_randomness` helper (+5 unit tests for the policy). Default for `Difficulty::Hard` flipped from strict-best (deterministic, repetitive) to `Randomness::SUBTLE` (top-3 within ±20 cp) so the AI varies between games — fixes user complaint "AI 幾乎會走一樣的步". Strength loss is imperceptible (±20 cp at depth 4 is below typical position-eval noise) and `KING_VALUE`=50_000 is way outside any preset's window, so all variation modes still defend the King correctly. Web: new `&variation=strict|subtle|varied|chaotic` URL param + picker radio fieldset; `parse`/`build_local_query` round-trip. TUI: new `--ai-variation <default|strict|subtle|varied|chaotic>` flag on `xiangqi --ai`. Tests that depended on Hard determinism now pass `randomness: Some(Randomness::STRICT)` explicitly. New benchmark integration test `tests/perf.rs` (#[ignore]) runs each Strategy×Difficulty across opening/midgame/endgame fixtures, reports nodes + median wall-clock + nodes/ms — invoked with `cargo test -p chess-ai --release perf -- --ignored --nocapture`. Results captured in `docs/ai/perf.md` with complexity model (`O(b^(3d/4))` practical), measured numbers (Hard=v3=opening=30ms, ~16k nodes; well under 250k budget), and headroom analysis (depth 5 ≈ 200ms, depth 6 ≈ 3s — depth 4 is the sweet spot until v4 ID+TT lands). README + v3 doc cross-link the new perf doc + randomness section.

- ✅ [2026-05-09] [P3/M] chess-ai v3 (king-safety) + move-history UI (web sidebar + TUI toggle) — Fixed casual-mode king-blindness bug where v1/v2 walked into 1-ply general-capture mates because `PieceKind::General => 0` in the eval (combined with casual rules' "no terminal status until general physically captured") meant losing the General was a 0-cp swing in the eval. v3 = `Strategy::MaterialKingSafetyPstV3` adds `KING_VALUE = 50_000` cp (>> Σ all other material ~9.6k cp, << MATE 1M cp), now default. Same search/PSTs/budget as v2 → strict superset of v2 strength, fixes one specific class of blunder. Regression test `v3_avoids_one_ply_general_capture_in_casual_mode` mirrors the user's bug report. Pitfall doc `pitfalls/casual-xiangqi-king-blindness.md` captures the class-of-bug ("don't lean on search terminals to model game-end states the eval can't see"). v1/v2 stay reachable via `?engine=v1|v2` (web) and `--ai-engine v1|v2` (TUI). Move-history UI: web sidebar gets a permanent `<MoveHistory>` panel (ICCS notation, side-coloured, scrollable, between Undo/New game and FX toggles); TUI gets a `H` toggle that renders a "History 棋譜 (N plies)" section in the sidebar with the last 24 plies. Helps debugging by letting users screenshot the move list directly. `docs/ai/v3-king-safety-pst.md` per-version spec, `docs/ai/README.md` updated table + roadmap renumbered (v4 = ID+TT, v5 = quiescence, v6 = WebWorker, v7 = ISMCTS for banqi, v8 = Pikafish backend). Workspace test/clippy/fmt/wasm32 all green.

- ✅ [2026-05-09] [P3/M] chess-ai v2 + modular strategy architecture + chess-tui vs-AI — Refactored `crates/chess-ai` from a single 352-line `lib.rs` into a strategy-pattern crate (`search/` + `eval/{material_v1,material_pst_v2}` + `engines/{NegamaxV1,NegamaxV2}`). New `Strategy` enum (`MaterialV1`, `MaterialPstV2`, default = v2) lets every consumer pick the engine version via `AiOptions::strategy`; older versions stay reachable bit-for-bit (switchable, non-overwriting). v2 = v1 search + 7 hand-rolled clean-room piece-square tables (±5..30 cp deltas) — fixes the "AI plays random openings" complaint without touching search code or node budgets. Public API `chess_ai::choose_move(state, opts)` unchanged. **chess-web**: `?engine=v1|v2` query param + picker radio inside the vs-AI fieldset; `Strategy::default()` (v2) when omitted. **chess-tui**: new `chess-ai` dependency, `PickerEntry::XiangqiVsAi` shortcut, `--ai / --ai-side / --ai-difficulty / --ai-engine` flags on the `xiangqi` subcommand, synchronous AI move pump in `apply_move` (native search at depth ≤ 4 is <100ms; Web Worker pump tracked as v5 in roadmap). vs-AI undo unmakes 2 plies in both clients so the human stays on move (mirrors the web fix from previous session). Versioned docs in `docs/ai/{README,v1-material,v2-material-pst}.md` — v1 doc preserves the original spec, v2 doc has per-table heuristics, README has a roadmap (v3 = ID+TT, v4 = quiescence+MVV-LVA, v5 = WebWorker, v6 = ISMCTS for banqi, v7 = optional Pikafish backend). `backlog/chess-ai-search.md` upgraded from stub to real version roadmap; `pitfalls/leptos-effect-tracking-stale-epoch.md` cross-linked to new docs. 13 chess-ai tests (4 inherited + 9 new: strategy-parameterised + PST shape sanity + dispatch distinguishability + parse round-trips), all 4 chess-web routes tests for `engine=` round-trips, full workspace `cargo test` + `cargo clippy -- -D warnings` + `wasm32-unknown-unknown` build clean.

- ✅ [2026-05-07] [P2/S] Victory effects + 將軍/CHECK toggle + picker hero (TUI & Web, protocol v4) — End-of-game gets a confetti burst (40+ particles, ~3 s) and a centered ASCII / SVG VICTORY/DEFEAT/DRAW banner overlaid on the board. Net players see personalised VICTORY/DEFEAT keyed off their seat; spectators and Local pass-and-play see neutral "Red Wins / Black Wins". chess-tui ships `--no-confetti` / `--no-check-banner` CLI flags; chess-web ships matching localStorage toggles (`chess.fx.confetti`, `chess.fx.checkBanner`) wired to two checkboxes in the sidebar footer. The "將軍 / CHECK" badge is now toggleable in both clients (default ON); chess-tui Net mode finally shows it (used to be Local-only). Wire change: `PlayerView` gains `pub in_check: bool` (`#[serde(default)]`, no-leak — banqi always reads false), bumps protocol to v4 — wire-compatible with v3 clients via the `serde(default)`. Picker polish: TUI gets a 5-row ASCII title (with single-line fallback in narrow terminals); Web gets a hero header with 帥 glyph + bilingual title + tagline. New: `clients/chess-tui/src/{banner,confetti}.rs`, `clients/chess-web/src/{prefs,components/end_overlay,components/confetti}.rs`, `docs/adr/0007-spectator-check-flag.md`. ASCII art is hand-rolled (no `figlet-rs` dep); confetti is xorshift32 + ratatui buffer cells (TUI) and 48 CSS-keyframed `<div>`s (web). Spectator share/replay link + bigger picker redesign tracked separately in P3.

- ✅ [2026-05-07] [P2/M] chess-net: spectator slots + in-room chat (protocol v3) — Third+ connection joining a room with `?role=spectator` upgrades to read-only spectator (`ServerMsg::Spectating` welcome instead of `Hello`); Move/Resign/Rematch/Chat from a spectator return `Error{"spectators cannot ..."}`. Players-only chat: `ClientMsg::Chat { text }` is rate-limit free for now but trimmed to ≤256 chars and stamped with `ts_ms` server-side; broadcast as `ServerMsg::Chat { line }` to seats + spectators and replayed to late joiners via `ServerMsg::ChatHistory` (50-line ring buffer, `VecDeque` per room). `RoomSummary` gains `spectators: u16` (`#[serde(default)]` for v2 back-compat); the lobby push includes the count and the chess-web lobby gains a Watch button while chess-tui gains a `w` lobby key. Spectator capacity defaults to 16 per room, `--max-spectators` / `CHESS_NET_MAX_SPECTATORS` to override. chess-tui press 't' to open a chat input editor (Enter sends, Esc cancels, players only). chess-web ships a sidebar `<ChatPanel>` with auto-scrolling log + disabled-for-spectators input. `make play-spectator` + `scripts/play-spectator.sh` boot a 2-player + 1-spectator tmux demo. ADR-0006 captures the spectator opt-in choice (`?role=spectator` rather than auto-fallback) for v2 back-compat. 8 new server-smoke tests + 5 new round-trip tests; v2 RoomSummary still decodes via `serde(default)`. Chat moderation, spectator chat permission per room, system messages (`ChatLine.from` enum widening to v4), reconnect-as-same-seat, and chess-web chat polish are separate P2 follow-ups.

- ✅ [2026-05-07] [P2/M] chess-web: Leptos + WASM frontend — `clients/chess-web` ships as a single-page Leptos+Trunk SPA covering all three variants (xiangqi local + online; banqi local + online; three-kingdom renders the empty 4×8 stub with a "WIP" overlay per CLAUDE.md). SVG-only rendering — no art assets — code-as-presentation, mirroring chess-tui's intersection layout. Both modes share one `<Board>` component: local mode owns `GameState` and projects `PlayerView` after every move; online mode receives `PlayerView` via `gloo-net` WS to `chess-net`'s `/ws/<room>` and `/lobby` endpoints. `orient.rs` + `glyph.rs` are byte-copies of the chess-tui modules with cloned tests (drift caught by both crates' CI). chess-net gains a `static-serve` feature (default-on) and `--static-dir <path>` flag that mounts `tower_http::services::ServeDir` as the route fallback so a single binary serves the SPA + WS in production; chess-web depends on chess-net with `default-features = false` so axum/tokio do not leak into the WASM build. New `make play-web` / `make build-web` / `make stop-web` targets + `scripts/play-web.sh` mirror the existing tmux harnesses. Trunk dev-server proxies `/ws*` and `/lobby` to chess-net at :7878. Reconnect / mobile layout / move animations / Playwright E2E / promote-orient-glyph-to-shared-crate are separate follow-ups in P2/P3.

- ✅ [2026-05-06] [P2/M] chess-net: multi-room lobby + optional password — `crates/chess-net` server now keys rooms by string id (`Arc<Mutex<HashMap<String, Arc<Mutex<RoomState>>>>>`). New routes: `GET /ws/<id>` (auto-creates the room), `GET /lobby` (live `ServerMsg::Rooms` push on every state change), `GET /rooms` (JSON snapshot for `curl`). Optional `?password=<secret>` query param locked in by the first joiner; mismatched joiners get `Error{"bad password"}` before any `Hello` and are dropped. Default room `main` (served from `/` and `/ws`) is permanent so v1 clients keep working. `chess-tui` adds `Screen::HostPrompt`, `Screen::Lobby`, `Screen::CreateRoom` plus the `Connect to server…` picker entry; `--lobby <ws>` and `--password <pw>` flags. Lobby uses a second sync `tungstenite` worker — no tokio added to the TUI. New ADR-0005, `make play-lobby` launcher, 8 server-smoke tests, 3 protocol round-trips. Mixed-variant servers / spectators / lobby filter / debouncing / admin tokens / TLS are separate P2 follow-ups.

- ✅ [2026-05-06] [P2/M] chess-net MVP: single-room websocket server + chess-tui --connect + tmux launcher — `crates/chess-net` ships an axum-ws server (`chess-net-server`) hosting one game/room. Wire schema: tagged JSON enums `ServerMsg` (Hello/Update/Error) and `ClientMsg` (Move/Resign), `Move::Reveal` stays `revealed: None` end-to-end. First connection = Red, second = Black, third gets "room full". `chess-tui --connect ws://...` runs a sync `tungstenite` worker thread + `std::sync::mpsc` channels (no tokio in TUI). `make play-local` / `scripts/play-local.sh` opens a tmux session with two clients in window 0 and the server in window 1. 8 new tests (6 protocol round-trip + 2 end-to-end smoke). Lobby / reconnect / time controls / takeback still deferred (kept as separate TODO items).

- ✅ [2026-05-06] [P2/M] chess-tui: ratatui frontend — interactive TUI for xiangqi + banqi. Vim cursor (hjkl) + arrows + mouse, Enter/Space select-or-commit, `u` undo, `f` flip (banqi). Two render styles: CJK glyphs (帥仕相俥傌炮兵 / 將士象車馬砲卒, default with color) and ASCII (--style ascii). Banqi displays in traditional 8×4 layout (transposed from 4×8 model). Variant picker on bare invocation; --as black flips xiangqi orientation for net-readiness. Engine stays presentation-free — orient.rs + glyph.rs live in the client.

- ✅ [2026-05-06] [P1/S] End-condition unit tests (checkmate / stalemate / banqi no-moves / no-progress) — tests/end_conditions.rs verifies refresh_status produces Won{Checkmate} / Won{Stalemate} / Won{OnlyOneSideHasPieces} from .pos fixtures. Pattern reusable for endgame puzzles.

Recently shipped. When implementing an active item, in the same commit run:

```
scripts/promote-todo.sh --title "<substring>" --summary "<one-line shipped summary>"
```

This moves the entry here using the dated `Done` syntax and re-validates.

- ✅ [2026-05-06] [P1/L] Foundational chess-core + workspace scaffolding — full xiangqi (perft-validated 1=44, 2=1920, 3=79666) + base banqi + CHAIN_CAPTURE/CHARIOT_RUSH house rules + PlayerView projection (no-leak proptest) + ICCS notation + chess-cli REPL. 8-crate workspace + WASM cleanliness in CI. 71 tests passing.

<!-- Prune older entries into CHANGELOG.md once prior-year items appear here
     or this section grows past ~20 entries. -->
